# Ubuntu 24.04 requires an explicit userns permission for unpackaged Chromium.
# The smoke harness sets TMPDIR to its temporary kavla-appimage-test directory.
profile kavla-desktop-smoke "/tmp/kavla-appimage-test-*/**/usr/lib/kavla-desktop/kavla-desktop" flags=(unconfined) {
  userns,
}
